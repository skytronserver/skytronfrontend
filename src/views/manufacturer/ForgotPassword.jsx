import React from 'react';
import { Box, Typography, Card, CardContent } from '@mui/material';

const ForgotPassword = () => {
  return (
    <Box p={3}>
      <Card>
        <CardContent>
          <Typography variant="h4" gutterBottom>ForgotPassword</Typography>
          <Typography variant="body1">This is a placeholder for the ForgotPassword page.</Typography>
        </CardContent>
      </Card>
    </Box>
  );
};

export default ForgotPassword;
