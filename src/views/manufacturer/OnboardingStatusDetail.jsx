import React from 'react';
import { Box, Typography, Card, CardContent } from '@mui/material';

const OnboardingStatusDetail = () => {
  return (
    <Box p={3}>
      <Card>
        <CardContent>
          <Typography variant="h4" gutterBottom>OnboardingStatusDetail</Typography>
          <Typography variant="body1">This is a placeholder for the OnboardingStatusDetail page.</Typography>
        </CardContent>
      </Card>
    </Box>
  );
};

export default OnboardingStatusDetail;
